interface MakeCounter {
  class: string;
  faction: string;
  unit_name: string;
  unit_type: string;
  unit_size: string;
  formation_id: string;
  special_forces: string;
  c_ref: number;
}

export default MakeCounter;
